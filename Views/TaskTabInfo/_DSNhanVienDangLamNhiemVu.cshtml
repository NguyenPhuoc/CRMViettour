@using CRM.Core
@using CRMViettour.Models
@using CRMViettour.Utilities
@model IEnumerable<tbl_TaskStaff>
    @{
        int i = 1;
    }
    <div class="table-scrollable table-custom-format">
        <table class="table table-striped table-bordered table-hover" id="">
            <thead>
                <tr>
                    <th>STT</th>
                    <th>Mã số</th>
                    <th>Họ tên</th>
                    <th>Vai trò</th>
                    <th>Diễn giải</th>
                    <th>Ngày giao</th>
                    <th>Người giao việc</th>
                    <th>#</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null)
                {
                    foreach (var item in Model)
                    {
                        <tr class="odd gradeX">
                            <td>@i</td>
                            <td></td>
                            <td>@item.tbl_Staff.FullName</td>
                            <td>@item.Role</td>
                            <td>@Html.Raw(item.Note)</td>
                            <td>@item.CreateDate.ToString("dd-MM-yyyy")</td>
                            <td>@item.tbl_StaffCreate.FullName</td>
                            <td><a style="cursor: pointer" onclick="confirm('Bạn thực sự muốn xóa những mục này ?'); deleteNVWork(@item.Id)"><i class="fa fa-trash"></i>&nbsp;Xóa</a></td>
                        </tr>
                        i++;
                    }
                }
            </tbody>
        </table>
    </div>
